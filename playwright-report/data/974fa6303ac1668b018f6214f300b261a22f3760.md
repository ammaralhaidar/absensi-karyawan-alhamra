# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.ts >> Login Flow >> Employee login redirects to dashboard
- Location: tests/e2e/login.spec.ts:5:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=QR Code Absensi')
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('text=QR Code Absensi')

```

```yaml
- heading "Alhamra Attendance" [level=1]
- paragraph: Sistem Absensi Karyawan
- text: Alhamra Attendance Sistem Absensi Karyawan Email
- textbox "Email":
  - /placeholder: nama@alhamra.com
- text: Password
- textbox "Password":
  - /placeholder: ••••••••
- button "Masuk"
- link "Lupa Password?":
  - /url: /forgot-password
- paragraph: "Demo Mode — Login langsung sebagai:"
- button "Karyawan Ahmad Fauzi"
- button "Security Hadi Wijaya"
- button "Admin Eka Wulandari"
- region "Notifications alt+T"
- alert
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | import { loginAs, accounts } from "./utils/auth";
  3  | 
  4  | test.describe("Login Flow", () => {
  5  |   test("Employee login redirects to dashboard", async ({ page }) => {
  6  |     await loginAs(page, accounts.employee.email, accounts.employee.password);
  7  |     await expect(page).toHaveURL(/\/dashboard/);
> 8  |     await expect(page.locator("text=QR Code Absensi")).toBeVisible({ timeout: 10000 });
     |                                                        ^ Error: expect(locator).toBeVisible() failed
  9  |   });
  10 | 
  11 |   test("Admin login redirects to admin", async ({ page }) => {
  12 |     await loginAs(page, accounts.admin.email, accounts.admin.password);
  13 |     await expect(page).toHaveURL(/\/admin/);
  14 |     await expect(page.locator("text=Dashboard")).toBeVisible({ timeout: 10000 });
  15 |   });
  16 | 
  17 |   test("Kiosk login redirects to kiosk", async ({ page }) => {
  18 |     await loginAs(page, accounts.kiosk.email, accounts.kiosk.password);
  19 |     await expect(page).toHaveURL(/\/kiosk/);
  20 |   });
  21 | 
  22 |   test("Invalid password shows error", async ({ page }) => {
  23 |     await page.goto("/login", { waitUntil: "networkidle" });
  24 |     await page.fill('input[type="email"]', accounts.employee.email);
  25 |     await page.fill('input[type="password"]', "wrongpassword");
  26 |     await page.click('button[type="submit"]');
  27 |     await page.waitForTimeout(2000);
  28 |     await expect(page.locator("text=Invalid")).toBeVisible({ timeout: 5000 });
  29 |   });
  30 | 
  31 |   test("Forgot password page loads", async ({ page }) => {
  32 |     await page.goto("/login", { waitUntil: "networkidle" });
  33 |     await page.click('text=Lupa Password?');
  34 |     await expect(page).toHaveURL(/\/forgot-password/);
  35 |     await expect(page.locator("text=Kirim Link Reset")).toBeVisible();
  36 |   });
  37 | });
  38 | 
  39 | test.describe("Login Flow Mobile", () => {
  40 |   test.use({ viewport: { width: 375, height: 812 } });
  41 | 
  42 |   test("Employee login on mobile", async ({ page }) => {
  43 |     await loginAs(page, accounts.employee.email, accounts.employee.password);
  44 |     await expect(page).toHaveURL(/\/dashboard/);
  45 |     await expect(page.locator("text=QR Code Absensi")).toBeVisible({ timeout: 10000 });
  46 |   });
  47 | });
  48 | 
```