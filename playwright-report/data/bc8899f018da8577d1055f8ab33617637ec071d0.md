# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: qr-code.spec.ts >> QR Code Generation >> QR code is visible on dashboard
- Location: tests/e2e/qr-code.spec.ts:10:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=QR Code Absensi')
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('text=QR Code Absensi')

```

```yaml
- heading "Alhamra Attendance" [level=1]
- paragraph: Sistem Absensi Karyawan
- text: Alhamra Attendance Sistem Absensi Karyawan Email
- textbox "Email":
  - /placeholder: nama@alhamra.com
- text: Password
- textbox "Password":
  - /placeholder: ••••••••
- button "Masuk"
- link "Lupa Password?":
  - /url: /forgot-password
- paragraph: "Demo Mode — Login langsung sebagai:"
- button "Karyawan Ahmad Fauzi"
- button "Security Hadi Wijaya"
- button "Admin Eka Wulandari"
- region "Notifications alt+T"
- alert
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | import { loginAs, accounts } from "./utils/auth";
  3  | import { cleanupTestData, getEmployeeIdByEmail, generateQRToken } from "./utils/supabase";
  4  | 
  5  | test.describe("QR Code Generation", () => {
  6  |   test.beforeEach(async () => {
  7  |     await cleanupTestData();
  8  |   });
  9  | 
  10 |   test("QR code is visible on dashboard", async ({ page }) => {
  11 |     await loginAs(page, accounts.employee.email, accounts.employee.password);
> 12 |     await expect(page.locator("text=QR Code Absensi")).toBeVisible({ timeout: 10000 });
     |                                                        ^ Error: expect(locator).toBeVisible() failed
  13 |     // QR SVG should load
  14 |     await page.waitForTimeout(3000);
  15 |     const svg = page.locator("svg");
  16 |     if (await svg.count() > 0) {
  17 |       await expect(svg.first()).toBeVisible();
  18 |     }
  19 |   });
  20 | 
  21 |   test("Timer countdown is visible", async ({ page }) => {
  22 |     await loginAs(page, accounts.employee.email, accounts.employee.password);
  23 |     await expect(page.locator("text=Refresh dalam")).toBeVisible({ timeout: 10000 });
  24 |   });
  25 | 
  26 |   test("QR token can be generated via API", async () => {
  27 |     const empId = await getEmployeeIdByEmail(accounts.employee.email);
  28 |     expect(empId).toBeTruthy();
  29 |     const token = await generateQRToken(empId, "check_in");
  30 |     expect(token).toBeTruthy();
  31 |     expect(token.split(".")).toHaveLength(3);
  32 |   });
  33 | });
  34 | 
```