# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: anomaly.spec.ts >> Anomaly Dashboard >> Anomaly table shows lupa checkout items
- Location: tests/e2e/anomaly.spec.ts:26:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=Anomali — Lupa Checkout')
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('text=Anomali — Lupa Checkout')

```

```yaml
- heading "Alhamra Attendance" [level=1]
- paragraph: Sistem Absensi Karyawan
- text: Alhamra Attendance Sistem Absensi Karyawan Email
- textbox "Email":
  - /placeholder: nama@alhamra.com
- text: Password
- textbox "Password":
  - /placeholder: ••••••••
- button "Masuk"
- link "Lupa Password?":
  - /url: /forgot-password
- paragraph: "Demo Mode — Login langsung sebagai:"
- button "Karyawan Ahmad Fauzi"
- button "Security Hadi Wijaya"
- button "Admin Eka Wulandari"
- region "Notifications alt+T"
- alert: Alhamra Attendance PWA
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | import { loginAs, accounts } from "./utils/auth";
  3  | import { cleanupTestData, supabase } from "./utils/supabase";
  4  | 
  5  | const today = () => new Date().toISOString().split("T")[0];
  6  | 
  7  | test.describe("Anomaly Dashboard", () => {
  8  |   test.beforeEach(async () => {
  9  |     await cleanupTestData();
  10 |     const { data: emp } = await supabase.from("employees").select("id").eq("email", "karyawan@ibsalhamra.sch.id").single();
  11 |     await supabase.from("attendance_logs").insert({
  12 |       employee_id: emp.id,
  13 |       date: today(),
  14 |       shift_id: "3330bd8f-0000-0000-0000-000000000000",
  15 |       check_in: new Date().toISOString(),
  16 |       check_out: null,
  17 |       status_in: "tepat_waktu",
  18 |       anomaly_flag: "lupa_checkout",
  19 |     });
  20 |   });
  21 | 
  22 |   test.afterEach(async () => {
  23 |     await cleanupTestData();
  24 |   });
  25 | 
  26 |   test("Anomaly table shows lupa checkout items", async ({ page }) => {
  27 |     await loginAs(page, accounts.admin.email, accounts.admin.password);
> 28 |     await expect(page.locator("text=Anomali — Lupa Checkout")).toBeVisible({ timeout: 10000 });
     |                                                                ^ Error: expect(locator).toBeVisible() failed
  29 |     await page.waitForTimeout(2000);
  30 |     const anomalyText = page.locator("text=Lupa Checkout");
  31 |     if (await anomalyText.count() > 0) {
  32 |       await expect(anomalyText.first()).toBeVisible();
  33 |     }
  34 |   });
  35 | 
  36 |   test("Resolve anomaly works", async ({ page }) => {
  37 |     await loginAs(page, accounts.admin.email, accounts.admin.password);
  38 |     await page.waitForTimeout(2000);
  39 |     const resolveBtn = page.locator("text=Resolve");
  40 |     if (await resolveBtn.count() > 0) {
  41 |       await resolveBtn.first().click();
  42 |       await expect(page.locator("text=Anomali berhasil diresolve")).toBeVisible({ timeout: 5000 });
  43 |     }
  44 |   });
  45 | });
  46 | 
```